# AI Activity Log

Record chronological AI activity, command runs, setup steps, and status checks here when those details need to be preserved.

Implemented user-visible project and feature changes belong in `status/changelog.md` instead.

## 2026-07-03

- Created root workspace `.gitignore`.
- Created workspace `README.md`.
- Created `repos.json` for Prowlarr, Sonarr, and Radarr.
- Created `scripts/setup.ps1` for cloning missing source repositories.
- Created `scripts/status.ps1` for checking source repository status.
- Created AI-oriented workspace structure and navigation files.
- Ran and documented a real Radarr local build: installed .NET SDK 8.0.421, activated Yarn 1.22.19 through Corepack, built backend for `win-x64`, and built the frontend production UI.
- Added project skill `.codex/skills/build-radarr/` so future Radarr build requests can follow the verified workflow directly.
- Ran and documented Radarr Windows installer generation, producing `Radarr.6.2.2.0.win-x64.exe` with Inno Setup 6.7.1.
- Created Prowlarr `my-feature` from latest tag `v2.5.0.5422`, built backend and frontend, and produced `Prowlarr.2.5.0.5422.win-x64.exe` with Inno Setup 6.7.1.
- Added project skill `.codex/skills/build-prowlarr/` so future Prowlarr build and installer requests can follow the verified workflow directly.
- Initialized this local workspace by running `scripts/setup.ps1`; cloned Prowlarr, Sonarr, and Radarr, and configured each upstream remote.
- Reclassified `status/changelog.md` as the implemented user-visible project and feature changelog; moved previous AI activity entries into this log file.
- Corrected initialization mistakes: restored `status/current.md` as the target state file, switched Prowlarr to local `my-feature` from tag `v2.5.0.5422`, switched Radarr to `my-feature` tracking `origin/my-feature`, and updated AI guidance so future initialization follows `current` without modifying it.
- Fetched Sonarr upstream tags and created local `my-feature` from latest tag `v4.0.19.2979`.
- Installed .NET SDK 6.0.428 for Sonarr, built Sonarr backend for `win-x64`, built frontend production UI, prepared the Windows installer input folder, and produced `Sonarr.my-feature.4.0.19.2979.win-x64-installer.exe` with the bundled Inno Setup 5 compiler.
- Added project skill `.codex/skills/build-sonarr/` so future Sonarr build and installer requests can follow the verified workflow directly.
- Began R001 implementation, choosing Prowlarr first because M-Team media metadata is the upstream field source for later Sonarr/Radarr sorting work.
- Implemented initial Prowlarr R001 support: parsed flexible M-Team audio/subtitle media fields, added structured release audio info, exposed audio/subtitle metadata through the search API and Newznab/Torznab output, and added search UI columns.
- Added a M-Team parser fixture covering English TrueHD and Chinese DDP 5.1 audio specs plus subtitle languages; the fixture was compiled by the backend build but not executed.
- Re-ran the documented Prowlarr backend publish build for `win-x64`; it passed with the known non-fatal Sentry API warning.
- Re-ran the documented Prowlarr frontend production webpack build; the first run generated missing CSS module typings and failed on those typings, then the second run passed.
- Read M-Team API wiki at `https://wiki.m-team.cc/zh-tw/api`; documented token-based `x-api-key` access, Swagger caveats, disallowed prefixes, and published rate limits without storing the raw token.
- Added `guides/mteam-api.md` and `.codex/skills/mteam-api/` with a PowerShell probe script for sanitized M-Team profile/search/detail/download-token requests.
- Validated the `mteam-api` skill with `quick_validate.py` and checked the PowerShell probe script with the PowerShell parser; no live M-Team API request was run.
- Generalized the `mteam-api` skill wording so it applies to all M-Team related development and real-data testing, not only one requirement.
- Ran live M-Team API probes with sanitized output under `tmp/`: profile plus search samples for `Interstellar`, `Inception`, and `Dune`.
- Observed that real `/api/torrent/search` responses do not contain structured `mediaInfo.audio` or `mediaInfo.subtitles`; search results expose summary fields including `audioCodec`, `videoCodec`, and `hasChineseSubtitle`.
- Ran live `/api/torrent/detail` probes for selected search result ids and confirmed full per-track audio/subtitle data appears in the detail `mediainfo` text field, using BDInfo `AUDIO`/`SUBTITLES` tables or MediaInfo `Audio #`/`Text #` sections.
- Fixed a strict-mode endpoint-string bug in `.codex/skills/mteam-api/scripts/Invoke-MTeamApiProbe.ps1` that affected detail/download-token output labeling.
- Updated the M-Team parser and fixture to parse real `mediainfo` text shapes, then re-ran the documented Prowlarr backend publish build for `win-x64`; it passed with the known non-fatal Sentry API warning.
- Explored production frontend static JS for alternate M-Team media endpoints; found related endpoints including `/torrent/files`, `/subtitle/list`, `/subtitle/search`, `/torrent/audioCodecList`, `/tracker/queryHistory`, and alternate API host `api.m-team.io`.
- Probed alternate M-Team endpoints with sanitized captures under `tmp/mteam-alt-api/`; none returned the per-track audio/subtitle metadata available in `/torrent/detail`'s `mediainfo`.
- Confirmed after user pointed it out that `/api/torrent/mediaInfo` returns the MediaInfo text directly for one torrent via `POST ?id=<torrent id>` or form body `id=<torrent id>`; no rate/quota headers were visible, and the official wiki does not publish a separate quota for this endpoint.
- Changed the R001 M-Team parser to fetch `/api/torrent/mediaInfo` by torrent id when search results lack parseable track metadata, added a parser fixture for that fallback path, and recorded the strategy in `planning/decisions.md`.
- Re-ran the documented Prowlarr backend publish build for `win-x64`; it passed with the known non-fatal Sentry API warning. Test assemblies compiled, but the test suite was not executed because test commands are not yet documented for this workspace.
- Updated R001 per user feedback: desktop and mobile Prowlarr search results now show compact media labels for multi-audio and multi-subtitle results, with full details in popovers; M-Team audio specification parsing now preserves Atmos markers from MediaInfo/BDInfo.
- Ran `corepack yarn run build --env production` from `Prowlarr/`; the first run failed on a strict TypeScript string-array narrowing issue in the new media display component, then passed after adding an explicit type guard.
- Attempted the documented Prowlarr backend publish build, but it did not start because `Prowlarr/global.json` requires .NET SDK `8.0.421` and the machine currently lists only `6.0.428`, `8.0.407`, and `9.0.203`.
- Installed official .NET SDK `8.0.421` into workspace-local `F:\arr-fork\.dotnet` using `dotnet-install.ps1`, added `/.dotnet/` to the root `.gitignore`, and used `F:\arr-fork\.dotnet\dotnet.exe` for Prowlarr compilation.
- Re-ran Prowlarr backend compile without installer generation: cleaned Debug/Release, then ran the documented `win-x64` publish build with NuGet v2 restore source; it passed and produced `_output/net8.0-windows/win-x64/publish/Prowlarr.exe`, `_output/net8.0/win-x64/publish/Prowlarr.Console.exe`, and `_output/Prowlarr.Update/net8.0/win-x64/publish/Prowlarr.Update.exe`.
- Re-ran Prowlarr frontend production webpack build without installer generation; it passed and produced `_output/UI/index.html`.
- Implemented Radarr R001 downstream support: added release `Subs` and `AudioInfo`, parsed Torznab/Newznab `subs` and `audio` attributes, exposed them from `/api/v3/release`, displayed compact media-info details in interactive search, and added a Torznab parser fixture for English TrueHD Atmos plus Chinese DDP 5.1.
- Verified Radarr R001 changes with `git diff --check`, backend Debug/Release clean, backend `win-x64` publish using workspace-local .NET SDK `8.0.421`, and frontend production webpack build after refreshing Yarn dependencies. Backend publish and frontend webpack passed; the test suite was not executed because test commands remain undocumented.
- Investigated slow Prowlarr M-Team searches after R001 and found the parser was issuing one synchronous `/api/torrent/mediaInfo` request for every search result that lacked embedded track metadata.
- Changed Prowlarr M-Team MediaInfo enrichment to cap requests to 20 results by default, run those requests with a maximum concurrency of 4, expose the cap as an advanced M-Team indexer setting, and add a parser fixture covering the cap.
- Verified the Prowlarr performance fix with `git diff --check` and the documented backend `win-x64` publish command using workspace-local .NET SDK `8.0.421`; backend publish passed with the known non-fatal Sentry warnings. The test suite was not executed because test commands remain undocumented.
- Reworked the M-Team MediaInfo performance fix after user feedback that only the first 20 results had full media metadata: default enrichment now covers 100 results, successful MediaInfo responses are cached for 7 days by API host and torrent id, MediaInfo requests use an explicit 1 second rate limit, and the existing 20-result cap is only used when configured in advanced settings.
- Re-ran the documented Prowlarr backend `win-x64` publish build after stopping the running test process that locked `_output`; the build passed with known non-fatal Sentry warnings. Restarted the compiled Prowlarr test build from `_output` with PID 109076, keeping the `_output/net8.0/win-x64/publish/UI` junction to `_output/UI` for frontend assets.

## 2026-07-04

- Reworked Prowlarr M-Team MediaInfo enrichment again per user feedback: initial search now returns pending rows immediately, `/api/v1/search/mediaInfo` enriches one cached release per request, and the frontend updates each release row in place with reused spinner indicators in audio/subtitle cells while follow-up requests are pending.
- Ran `git diff --check` for Prowlarr; it passed with only CRLF normalization warnings.
- Ran the documented Prowlarr frontend production webpack build with `corepack yarn run build --env production`; it passed after using elevated execution for Corepack's user-directory access. Removed Corepack's automatic `packageManager` side-effect from `package.json`.
- Stopped the previously running compiled Prowlarr test process PID 109076, re-ran documented backend Debug/Release clean and `win-x64` publish with workspace-local .NET SDK `8.0.421`; publish passed with the known non-fatal Sentry warnings.
- Restarted the compiled Prowlarr test build from `_output` with PID 106716 and confirmed `http://localhost:9696/login` returns HTTP 200.

## 2026-07-07

- Updated Prowlarr R001 search UI per user feedback: fixed audio/subtitle desktop table header alignment, made audio/subtitle columns manually sortable, kept pending or empty media-info rows last, and added footer progress/completion text for MediaInfo enrichment.
- Verified localization JSON for English and Simplified Chinese with `ConvertFrom-Json`.
- Ran Prowlarr frontend production webpack build with `COREPACK_ENABLE_AUTO_PIN=0; corepack yarn run build --env production`; it passed.
- Ran `git diff --check` for Prowlarr; it passed with only CRLF normalization warnings.
- Built a new Prowlarr Windows version from `Prowlarr/`: confirmed .NET SDK `8.0.421`, Yarn `1.22.19`, Node `v22.16.0`, and Inno Setup `6.7.1`; ran locked Yarn install, backend Debug/Release clean plus `win-x64` publish, frontend production webpack, package folder assembly, UI copy into publish output, and Inno installer generation.
- Generated `Prowlarr.2.5.0.5422.win-x64.exe` at `Prowlarr/distribution/windows/setup/output/`; size `75066874` bytes, SHA256 `7C01598B0FC8E2E669D4AF83DFB4541DC9EB11FFFF7512017B862D02631CD1FD`.
- Fixed the Prowlarr runnable-output workflow after the user hit a JSON 404 from `_output\net8.0\win-x64\Prowlarr.Console.exe`: copied `_output\UI` into non-publish and publish output folders plus `_artifacts\win-x64\net8.0\Prowlarr`, verified `http://localhost:9696/` returned `200 text/html`, and updated `.codex/skills/build-prowlarr/SKILL.md` so future local-run builds copy UI beside all runnable executables.
- Added R001 Custom Filter support for Prowlarr search-result audio/subtitle fields and moved MediaInfo enrichment progress into backend release/search state; `/api/v1/search` and `/api/v1/search/mediaInfo` resources now carry `mediaInfoSearchId`, `mediaInfoProgressStatus`, `mediaInfoProgressCompleted`, and `mediaInfoProgressTotal`, and Torznab/Newznab output emits matching attrs when MediaInfo progress applies.
- Re-verified Prowlarr with `git diff --check`, localization JSON parsing, frontend production webpack, backend Debug/Release clean plus `win-x64` publish, then copied UI output into all direct-run output folders.
- Recompiled Prowlarr runnable output only, without generating a new installer: confirmed .NET/Yarn/Node prerequisites, ran locked Yarn install, backend Debug/Release clean plus `win-x64` publish, frontend production webpack, and copied `_output\UI` into all direct-run output folders.
- Added R005 draft requirement for Custom Filter OR condition support. Initial code search confirmed Prowlarr, Sonarr, and Radarr all have Custom Filter API/frontend Filter Builder paths, and their client-side collection selectors currently show AND-style sequential predicate evaluation.
- Updated R001 requirements before implementation to make Radarr downstream behavior explicit: Radarr must receive audio/subtitle/search-state fields from Prowlarr, show pending row loading and whole-result progress, keep row updates from refreshing table sort, and wait before automatic tracking decisions.
- Implemented Radarr R001 media-info progress consumption: parsed Prowlarr Torznab/Newznab `mediaInfoStatus`, `mediaInfoSearchId`, and progress attrs; exposed them from `/api/v3/release`; added interactive-search row loading/progress display and stable MediaInfo sorting snapshots; and made non-interactive searches wait for pending media info before download decisions continue, with timeout protection.
- Verified Radarr changes with `git diff --check`, localization JSON parsing for English/Simplified Chinese files, frontend production webpack build, and backend `win-x64` publish build. Test assemblies compiled during publish, but the test suite was not executed because test commands remain undocumented.
- Recompiled Radarr on user request using the verified Windows workflow: confirmed .NET SDK `8.0.421` and Yarn `1.22.19`, ran locked Yarn install, removed `_output`/`_tests`, ran backend Debug/Release clean plus `win-x64` publish, and ran frontend production webpack. Backend and frontend builds passed; test assemblies compiled but tests were not executed.
- Refined Radarr interactive search after user noted the new fields lacked dedicated columns: replaced the combined MediaInfo column with separate Audio Info, Subtitle Languages, and Additional Data columns; added sorting/filter metadata for those fields; and re-ran frontend production webpack plus backend `win-x64` publish successfully.
- Recompiled Radarr again on user request using the verified Windows workflow: confirmed .NET SDK `8.0.421` and Yarn `1.22.19`, ran locked Yarn install, removed `_output`/`_tests`, ran backend Debug/Release clean plus `win-x64` publish, and ran frontend production webpack. Backend and frontend builds passed; test assemblies compiled but tests were not executed.
- Investigated Radarr interactive search still showing no audio/subtitle columns in the running instance. Found the process was running from `Radarr/_artifacts/win-x64/net8.0/Radarr`, while the latest build output was under `_output`; synchronized the new backend publish files and `_output/UI` into the running `_artifacts` directory, restarted `Radarr.Console.exe`, and confirmed the active `UI/index.html` now references the new `index-8e27159d28c53c642385.js` bundle containing `audioInfo`, `SubtitleLanguages`, `AdditionalData`, and `mediaInfoStatus`.
- Investigated Radarr interactive search staying at `Waiting for additional data 0/61`. Found Radarr was only polling `/api/v3/release`, which can read already-updated Prowlarr state but does not trigger Prowlarr's per-row `/api/v1/search/mediaInfo` enrichment. Added Radarr parsing/storage for Prowlarr's source indexer id, added a backend `POST /api/v3/release/mediaInfo` proxy to Prowlarr, changed the frontend to run a per-row enrichment queue, and updated automatic searches to actively enrich pending rows before decisions. Verified with `git diff --check`, frontend production webpack, backend `win-x64` publish, then synchronized `_output` into the running `_artifacts` directory and restarted Radarr.
- Investigated Radarr interactive search immediately marking all additional data as failed while progress stayed at `0/61`. Confirmed Radarr was reaching Prowlarr, but Prowlarr returned `404` from `/api/v1/search/mediaInfo` because Torznab/Newznab downstream searches were not cached in the same release cache used by Prowlarr's own search UI. Updated Prowlarr's Newznab/Torznab controller to cache downstream search releases with shared MediaInfo progress state, rebuilt backend `win-x64`, restarted the running Prowlarr publish instance, and verified a real M-Team downstream search followed by `/api/v1/search/mediaInfo` returned `200`, `available`, and progress `1/61 pending`.
- Applied Radarr interactive-search UI feedback: removed the dedicated Additional Data table column while keeping the header progress alert, changed audio/subtitle loading indicators to disappear per row once that cell has returned data, and changed audio/subtitle cells to compact labels with full details in hover popovers. Re-ran `git diff --check`, rebuilt Radarr frontend production UI successfully, and copied `_output/UI` into the running `_artifacts/win-x64/net8.0/Radarr/UI` folder.
- After the user still saw the Additional Data column after a browser refresh, removed `mediaInfoStatus` from the media-info sort-key list so the frontend business chunk fingerprint changed, rebuilt Radarr frontend production UI again, copied the new `527-b10217621c5711636b61.js` chunk into the running `_artifacts` UI folder, restarted Radarr, and verified from the running source map that Interactive Search columns now go from Subtitle Languages directly to Quality with no `mediaInfoStatus`/Additional Data table column.
- Updated R005 per user clarification: Custom Filter should support nested AND/OR condition groups such as `(条件1 or 条件2) and (条件3 or 条件4)` and `(条件1 and 条件2) or 条件3`, with UI expectations based on the provided grouped-condition sketch.
- Implemented R005 in Prowlarr and Radarr, leaving Sonarr deferred per user request: added nested Custom Filter condition groups in the Filter Builder UI, recursive client-side condition-tree evaluation, legacy flat-AND serialization for simple filters, group-aware typed filter state, and protection so server-side query filters are only pushed down when the selected filter remains flat AND-compatible.
- Verified the R005 frontend changes with targeted ESLint and production webpack builds for both Prowlarr and Radarr. Backend builds were not re-run because this implementation did not change backend code or database schema.
- Recompiled Radarr on user request using the verified Windows workflow: confirmed .NET SDK `8.0.421`, Yarn `1.22.19`, and Node `v22.16.0`; ran locked Yarn install, removed `_output`/`_tests`, ran backend Debug/Release clean plus `win-x64` publish, and ran frontend production webpack. Synchronized the new output into `Radarr/_artifacts/win-x64/net8.0/Radarr`, replaced the running Radarr process, and verified `http://localhost:7878/` returned `200 text/html`.
- Recompiled Prowlarr on user request using the verified Windows workflow: confirmed branch `my-feature` at tag `v2.5.0.5422`, stopped the running `_output` publish process, ran locked Yarn install, removed `_output`/`_tests`, ran backend Debug/Release clean plus `win-x64` publish with NuGet v2 restore source and `NuGetAudit=false`, ran frontend production webpack, copied UI into direct-run output folders, restarted Prowlarr from `_output/net8.0/win-x64/publish`, and verified `http://localhost:9696/` returned `200 text/html`.
- Fixed R005 follow-up issues in Prowlarr and Radarr: group-header add-condition/add-group buttons now append to the current condition group instead of inserting beside the group in its parent, and per-row MediaInfo responses now merge into the full release list before progress/filter state is recalculated so rows hidden by an active filter can appear once their additional data satisfies that filter. Prowlarr also gained an Additional Data custom-filter field.
- Verified the follow-up fixes with `git diff --check` for both Prowlarr and Radarr, then ran frontend production webpack builds for both repositories; both builds passed. Targeted ESLint was attempted but single-file linting of `releaseActions.js` reports existing legacy rule noise such as `no-use-before-define`/`no-loop-func`, so the production builds were used as the final frontend verification. Prowlarr's publish `UI` path is a junction to `_output/UI`, and the Radarr `_artifacts/win-x64/net8.0/Radarr` UI files were refreshed from the build output.
- Updated `INDEX.md` and the Prowlarr/Radarr/Sonarr build skills with the user's local run policy: before starting a compiled build, identify and stop the existing stable installed process; when ending a compiled build, only stop it if it is running from the workspace output/artifact paths, then restart the recorded stable installed process or service. Also corrected stale `G:\arr-fork` paths in the Prowlarr/Radarr build skills to the active `F:\arr-fork` workspace.
- Recompiled Prowlarr and Radarr on user request. Prowlarr used workspace-local .NET SDK `8.0.421`, Yarn `1.22.22` through Corepack with `COREPACK_ENABLE_AUTO_PIN=0`, NuGet v2 restore source plus `NuGetAudit=false`, backend Debug/Release clean plus `win-x64` publish, frontend production webpack, and UI copy into direct-run output folders. The first Prowlarr backend attempt failed because the running workspace `Prowlarr.Console.exe` locked `_output`; stopped only that workspace process, reran publish successfully, restarted Prowlarr from `_output/net8.0/win-x64/publish`, and confirmed `http://localhost:9696/` returned `200 text/html`.
- Recompiled Radarr with workspace-local .NET SDK `8.0.421`, Yarn `1.22.22` through Corepack with `COREPACK_ENABLE_AUTO_PIN=0`, locked Yarn install, backend Debug/Release clean plus `win-x64` publish, and frontend production webpack. Verified `_output/net8.0-windows/win-x64/publish/Radarr.exe`, `_output/net8.0/win-x64/publish/Radarr.Console.exe`, and `_output/UI/index.html` exist. The installed Radarr process at `C:\ProgramData\Radarr\bin\Radarr.Console.exe` was left running and untouched.
- Fixed a Prowlarr search footer progress bug where `getMediaInfoProgress` used the first release carrying `mediaInfoProgressStatus`, leaving the UI stuck on stale counts such as `Querying additional data 1/100` after later rows returned. The frontend now selects the highest completed progress value across current releases. Verified with `git diff --check`, Prowlarr frontend production webpack, refreshed `_output/UI` into direct-run output folders, and confirmed `http://localhost:9696/` returned `200 text/html`.

## 2026-07-08

- Stopped the running workspace-compiled Prowlarr process at `F:\arr-fork\Prowlarr\_output\net8.0\win-x64\publish\Prowlarr.Console.exe`, started the installed `Prowlarr` Windows service from `C:\ProgramData\Prowlarr\bin\Prowlarr.Console.exe`, and verified Prowlarr, Radarr, and Sonarr all returned `200 text/html` from their local web UIs. Radarr was already running from `C:\ProgramData\Radarr\bin\Radarr.Console.exe`; Sonarr was already running from `C:\ProgramData\Sonarr\bin\Sonarr.exe`.
- Created a static HTML Radarr architecture wiki at `wiki/radarr-architecture/index.html`, with top-down module breakdowns, module organization diagrams, runtime flow diagrams, and links to the local Radarr source files.
- Added `scripts/Measure-MTeamMediaInfoRateLimit.ps1` for cautious `/api/torrent/mediaInfo` rate-limit probing with token read from `MTEAM_API_TOKEN`, sanitized JSON/CSV output, baseline detection, recovery measurement, optional zero-delay burst, and interval sweep.
- Re-ran live M-Team `/api/torrent/mediaInfo` rate-limit probes after the earlier limited state cleared. A baseline request at 2026-07-08 00:40:23 +08:00 succeeded. Interval sweeps of 60/45/30 seconds, then 20/15 seconds, then 10 seconds, then 5 seconds, then 3 seconds all succeeded for three attempts per interval. A zero-delay burst of 8 requests also succeeded. A later zero-delay burst of 20 requests succeeded through attempt 19 and returned `request too frequent` on attempt 20; a recovery probe 60 seconds later succeeded.
- Continued probing `/api/torrent/mediaInfo` limits. Two-second and one-second intervals succeeded for five attempts each, and a one-second interval sustained 20 attempts plus baseline without rate limiting. A later zero-delay burst triggered rate limiting on attempt 18, and 30 seconds later was still limited; another probe roughly 60 seconds from the original trigger was still limited. After a 120-second quiet wait the endpoint was still limited, then recovered after a later 300-second quiet wait. A fresh zero-delay burst of 25 did not trigger, but a later burst with a 50-request cap triggered at attempt 13; 180 seconds, another 300 seconds, and another 600 seconds of quiet wait still returned `request too frequent`. Conclusion from this run: small fixed intervals down to 1 second can work for limited batches, but recovery duration is not fixed and can exceed 10 minutes after stronger or repeated rate-limit events.
- Documented the M-Team `/api/torrent/mediaInfo` rate-limit lessons in `guides/mteam-api.md`, `.codex/skills/mteam-api/references/api-notes.md`, `.codex/skills/mteam-api/SKILL.md`, and `status/current.md`, including HTTP 200 rate-limit detection, stateful burst thresholds, non-deterministic recovery, and recommended single-concurrency plus long-backoff behavior.
- Ran the requested long 1-second interval `/api/torrent/mediaInfo` probe after 10 minutes of quiet time. Baseline succeeded, then one-second interval attempts 1-24 succeeded and attempt 25 returned `請求過於頻繁`; recovery probing was intentionally skipped to avoid refreshing the penalty window. Sanitized JSON/CSV were saved under `tmp/mteam-mediainfo-rate-limit/`.
- Ran the requested 2-second interval `/api/torrent/mediaInfo` probe with a 200-attempt cap. Baseline succeeded, interval attempts 1-48 succeeded, and attempt 49 returned `請求過於頻繁`; recovery probing was intentionally skipped. Sanitized JSON/CSV were saved under `tmp/mteam-mediainfo-rate-limit/`.
- Ran the requested 3-second interval `/api/torrent/mediaInfo` probe with a 200-attempt cap. Baseline succeeded, interval attempts 1-49 succeeded, and attempt 50 returned `請求過於頻繁`; recovery probing was intentionally skipped. Sanitized JSON/CSV were saved under `tmp/mteam-mediainfo-rate-limit/`.
- Ran the requested 5-second interval `/api/torrent/mediaInfo` probe with a 200-attempt cap. Baseline succeeded, interval attempts 1-24 succeeded, and attempt 25 returned `請求過於頻繁`; recovery probing was intentionally skipped. Sanitized JSON/CSV were saved under `tmp/mteam-mediainfo-rate-limit/`.
- Added `scripts/Measure-MTeamMediaInfoCycleLimit.ps1` for the requested cycle/backoff probe: active phase uses 1-second spacing until a non-success response, then waits 5 minutes, increments the backoff by 1 minute after failed recovery checks, and stops at the requested total request count.
- Ran the cycle/backoff `/api/torrent/mediaInfo` probe to 100 total requests. Cycle 1 succeeded through request 50 and failed at request 51; the 5-minute backoff check at request 52 succeeded. Cycle 2 then failed at request 77 after 25 more active successes; the 5-minute backoff check at request 78 succeeded. Cycle 3 continued successfully through total request 100. Final result: 98 successes, 2 `請求過於頻繁` responses, and sanitized JSON/CSV saved under `tmp/mteam-mediainfo-rate-limit/`.
- Ran live M-Team `/api/torrent/mediaInfo` probes against one `Titanic` search result id. The endpoint returned HTTP 200 with API `code=1`, message `請求過於頻繁`, and empty `data` from the baseline request onward. Zero-delay requests and 3/5/8/10/15-second intervals all returned the same message while the token/IP was already in the limited state. Recovery probes every 30 seconds for 5 minutes and every 60 seconds for 10 minutes also returned `請求過於頻繁`; a final sanitized result was saved under `tmp/mteam-mediainfo-rate-limit/`.
- Investigated a Prowlarr M-Team search where all Titanic rows showed `-` for audio/subtitles despite known MediaInfo availability. `C:\ProgramData\Prowlarr\logs\prowlarr.debug.txt` showed the search at 2026-07-07 23:47 and 23:48 returned 100 reports, then nearly every `/api/torrent/mediaInfo` follow-up failed with M-Team API message `請求過於頻繁` (`request too frequent`). Fixed the behavior by reducing frontend MediaInfo enrichment to one request at a time, retrying rows that remain pending, changing M-Team mediaInfo fetches to use the indexer's 5-second rate limit, preserving `pending` on retryable rate-limit responses instead of converting to `unavailable`, and only incrementing backend progress when a row leaves `pending`. Verified with `git diff --check`, Prowlarr frontend production webpack, backend `win-x64` publish, UI copy into direct-run folders, restart from `_output/net8.0/win-x64/publish`, and `http://localhost:9696/` returning `200 text/html`.
- Added `-SuccessTarget` to `scripts/Measure-MTeamMediaInfoCycleLimit.ps1` so long MediaInfo cycle/backoff probes can stop at a requested count of successful responses while keeping a separate safety cap for total requests.
- Ran the requested 1-second active cycle/backoff `/api/torrent/mediaInfo` probe until 200 successful responses. The run started at 2026-07-08 09:01:11 +08:00 and ended at 10:12:07 +08:00 with 209 total MediaInfo requests, 200 successes, and 9 `request too frequent` responses. Active cycles 1, 2, and 3 each failed on attempt 51. The first two 5-minute backoff checks succeeded; after the third failure, 5-, 6-, 7-, 8-, 9-, and 10-minute checks remained limited, and the 11-minute check succeeded. Sanitized JSON/CSV were saved under `tmp/mteam-mediainfo-rate-limit/`.
- Ran the requested 10-second active cycle/backoff `/api/torrent/mediaInfo` probe until 360 successful responses. The run started at 2026-07-08 10:26:23 +08:00 and ended at 12:40:23 +08:00 with 370 total MediaInfo requests, 360 successes, and 10 `request too frequent` responses. The first active cycle failed on attempt 101 and recovered on the 9-minute backoff check after 5-, 6-, 7-, and 8-minute checks failed. The second active cycle failed on attempt 151 and recovered with the same 9-minute pattern. The third active cycle reached the success target without another failure. Sanitized JSON/CSV were saved under `tmp/mteam-mediainfo-rate-limit/`.
- Ran a cross-endpoint M-Team probe to see whether `/api/torrent/mediaInfo` rate limiting blocks `/api/torrent/detail` for the same token. A baseline `/api/torrent/detail?id=<torrent id>` request succeeded and returned `mediainfo`. Then 1-second-spaced `/api/torrent/mediaInfo` requests succeeded through attempt 50 and returned `請求過於頻繁` on attempt 51. An immediate follow-up `/api/torrent/detail?id=<torrent id>` request still succeeded and returned `mediainfo`. A sanitized summary was saved under `tmp/mteam-mediainfo-rate-limit/`.
- Ran a focused `/api/torrent/detail` rate-limit probe. The baseline detail request succeeded, 1-second-spaced interval attempts 1-49 succeeded, and interval attempt 50 returned `請求過於頻繁`; sanitized JSON/CSV were saved under `tmp/mteam-mediainfo-rate-limit/`. Immediately afterward, a single `/api/torrent/mediaInfo?id=<torrent id>` request still returned `SUCCESS`, supporting the conclusion that detail and mediaInfo are not sharing one immediate limiter bucket.
- Added `scripts/Measure-MTeamDualEndpointRateLimit.ps1` for alternating `/api/torrent/mediaInfo` and `/api/torrent/detail` probes with a global request interval, per-endpoint minimum interval, per-endpoint cooldown, sanitized JSON/CSV output, and total-success target.
- Started a dual-endpoint probe with one request every 10 seconds, each endpoint at a 20-second minimum interval, 12-minute endpoint-specific cooldown, and a 500-success target. The first run was stopped because Windows PowerShell misread a non-ASCII message matcher in the new script, causing `請求過於頻繁` to be classified as `api_non_success` instead of entering cooldown; fixed the script with an ASCII fallback that treats fixed-endpoint `code=1`, non-`SUCCESS`, empty-data responses as rate limiting.
- Re-ran the corrected dual-endpoint probe. It started at 2026-07-08 15:14:00 +08:00 and ended at 17:06:16 +08:00 after reaching 500 successful responses in 506 total requests. `/api/torrent/mediaInfo` had 253 requests, 250 successes, and 2 rate-limit responses; `/api/torrent/detail` had 253 requests, 250 successes, and 2 rate-limit responses. Two transient HTTP 502 transport errors were observed and did not count as successes or cooldown events. Sanitized JSON/CSV were saved under `tmp/mteam-mediainfo-rate-limit/`.
- Ran a same-account token-switch probe for `/api/torrent/mediaInfo`. Two provided tokens both succeeded at baseline. Token A then returned `請求過於頻繁` on the 49th 1-second-spaced trigger attempt; an immediate request with token B also returned `請求過於頻繁`, and a confirmation request with token A remained limited. Sanitized JSON/CSV were saved under `tmp/mteam-mediainfo-rate-limit/`. Conclusion: switching tokens for the same account did not bypass the observed MediaInfo limiter, although the probe cannot distinguish account-level from IP-level limiting.
- Solidified the M-Team R001 additional-data scheduler design in `planning/decisions.md` D002 and added the corresponding implementation next step to `status/current.md`: endpoint-aware MediaInfo/Detail requesters, three queues, identical rate-limit rollback for both endpoints, windowed task creation, handle lease/cancel semantics, Prowlarr APIs, and Radarr/Sonarr backend-owned handle management. No application code was changed.
- Implemented the R001 M-Team additional-data scheduler design in Prowlarr: added a singleton in-memory scheduler with MediaInfo/detail endpoint workers, explicit MediaInfo/detail queues plus a shared additional-data queue, 20-second endpoint pacing, 12-minute endpoint-specific cooldown, identical rate-limit rollback to the original queue front for both endpoints, 5-minute handle leases, cancel support, and MediaInfo/detail fetch paths sharing the same successful MediaInfo cache.
- Updated Prowlarr search media-info APIs and frontend polling for handle-based, windowed additional-data enrichment: `/api/v1/search/mediaInfo` now creates or renews scheduler handles, `/api/v1/search/mediaInfo/cancel` cancels active handles, release resources carry `mediaInfoHandleId`, and the search UI keeps at most four active tasks while advancing the window one completed row at a time.
- Updated Radarr downstream handling for the new Prowlarr scheduler: release resources carry `mediaInfoHandleId`, manual interactive search proxies handle ids and cancellation through `/api/v3/release/mediaInfo` and `/api/v3/release/mediaInfo/cancel`, the frontend keeps at most four active additional-data handles, and automatic search enrichment uses the same window while continuing to wait for required pending candidates before making final decisions or timing out.
- Verified the scheduler implementation with `git diff --check` for Prowlarr and Radarr, backend `win-x64` publish builds for Prowlarr and Radarr using workspace-local .NET SDK `8.0.421`, and production frontend webpack builds for Prowlarr and Radarr. The test suite was not executed because test commands remain undocumented.
- Added debug logging for the R001 additional-data chain: Prowlarr backend now logs scheduler task/handle create, renew, claim, execute, rate-limit rollback, cancel, and endpoint request start/cache/result events; Prowlarr frontend logs windowed polling and handle activity with a `[Prowlarr MediaInfo]` console prefix; Radarr backend logs manual proxy and automatic-search Prowlarr task activity; Radarr frontend logs the same windowed polling events with a `[Radarr MediaInfo]` console prefix. Re-verified with `git diff --check`, Prowlarr/Radarr frontend production webpack builds, and Prowlarr/Radarr backend `win-x64` publish builds.
- Added backend request-source context to the R001 additional-data logs after the user clarified that browser console output will not be used for diagnostics. Prowlarr mediaInfo/cancel API logs now include source and host; Radarr mediaInfo/cancel proxy logs include remote IP and host. Re-ran Prowlarr and Radarr backend `win-x64` publish builds successfully after this logging adjustment.
- Recompiled and ran the workspace builds for Prowlarr and Radarr on user request. Both repos used workspace-local .NET SDK `8.0.421`, Corepack Yarn `1.22.22`, locked Yarn install, backend Debug/Release clean plus `win-x64` publish, frontend production webpack, and UI copy into direct-run output folders. Stopped the installed Windows services `Prowlarr` and `Radarr`, then started `F:\arr-fork\Prowlarr\_output\net8.0\win-x64\publish\Prowlarr.Console.exe` and `F:\arr-fork\Radarr\_output\net8.0\win-x64\publish\Radarr.Console.exe`. Verified `http://localhost:9696/` and `http://localhost:7878/` both returned `200 text/html`.
- Reviewed live Prowlarr/Radarr additional-data logs after the user reported handle串流 and stalled progress. The logs showed duplicated torrents shared one internal task correctly, but shared release caches could return another business party's handle on cache hits, e.g. Prowlarr UI requests receiving Radarr handles after a task completed. The apparent "two by two" responses matched the two endpoint workers consuming the shared additional-data queue with separate 20-second endpoint pacing.
- Fixed the R001 handle lifecycle issue: Prowlarr scheduler tasks now support multiple handles observing the same terminal result, renewals refresh both handle TTL and task TTL, mismatched handle/task renewals are ignored, queued/rate-limited tasks drop when no active handles remain, and running tasks drop their result if every handle expired or was canceled by completion time. Prowlarr now clones cached releases for `/api/v1/search/mediaInfo` and keeps caller handles out of the shared cache; Radarr no longer stores terminal Prowlarr handles in its backend release cache.
- Rebuilt and restarted the workspace Prowlarr/Radarr pair after the handle fix. Prowlarr backend `win-x64` publish and frontend production webpack passed; Radarr backend `win-x64` publish and frontend production webpack passed with the known non-fatal ffprobe retry/Sentry/Browserslist warnings. Copied UI output into direct-run folders, started Prowlarr PID `157884` and Radarr PID `173840` from `_output/net8.0/win-x64/publish`, and verified `http://localhost:9696/` and `http://localhost:7878/` returned `200 text/html`.
- Checked new startup logs after the restart. Prowlarr started from `F:\arr-fork\Prowlarr\_output\net8.0\win-x64\publish\Prowlarr.Console.exe` at 2026-07-08 23:19:16 and reached `Application started` at 23:19:39; Radarr started from `F:\arr-fork\Radarr\_output\net8.0\win-x64\publish\Radarr.Console.exe` at 23:19:16 and reached `Application started` at 23:19:16. No new 23:19 startup Warn/Error/Fatal entries were found for either app.

## 2026-07-09

- Updated `AGENTS.md` per user request so future completed implementation requests and bug fixes are committed directly after status review and feasible documented verification, with separate commits per touched source repository and unrelated changes excluded.
- Fixed the remaining R001 progress-count isolation bug: Prowlarr progress entries now keep a per-search completed release-key set, so completion of a shared torrent by another Prowlarr/Radarr session no longer prevents the current search session from incrementing its own count. Prowlarr also initializes additional-data progress as completed rows over full result rows, and Radarr/Prowlarr frontends use full result count in their fallback progress calculation.
- Moved Radarr interactive-search additional-data progress from the table body alert into `MovieInteractiveSearchModalContent`'s `ModalFooter`, with a small footer progress area beside the close button.
- Verified with `git diff --check` for Prowlarr and Radarr, Prowlarr backend `win-x64` publish, Prowlarr frontend production webpack, Radarr backend `win-x64` publish, and Radarr frontend production webpack. Copied UI output into direct-run output folders, restarted Prowlarr PID `180364` and Radarr PID `80648` from `_output/net8.0/win-x64/publish`, and confirmed `http://localhost:9696/` and `http://localhost:7878/` returned `200 text/html`.
- Refined Prowlarr Audio/Subtitle sort snapshots to include a MediaInfo sort state group: actual values, completed empty values, and pending/not-yet-requested rows now sort separately. Verified with Prowlarr `git diff --check`, frontend production webpack, copied `_output/UI` into direct-run output folders, and confirmed the running Prowlarr workspace process PID `180364` still served `http://localhost:9696/` as `200 text/html`.
- Implemented the user's Radarr Chinese media preference over R001 MediaInfo fields: added a Chinese media preference evaluator, non-interactive temporary pending-MediaInfo and permanent Chinese-accessibility decision specs, selected-audio scoring for Atmos/channel/lossless-or-lossy codec class, release ordering by audio preference score after quality and Custom Format score, release API fields for selected audio/score/Chinese accessibility, and focused fixtures for evaluator/spec/sorting behavior. Interactive pending rows avoid a temporary rejection so in-place MediaInfo enrichment does not leave stale rejection state. Verified with `git diff --check` and Radarr backend `win-x64` publish using .NET SDK `8.0.421`; test assemblies compiled but the test suite was not executed because test commands remain undocumented.
- Recompiled and ran the workspace Prowlarr/Radarr pair on user request. Prowlarr used .NET SDK `8.0.421`, Corepack Yarn `1.22.19`, locked Yarn install, backend Debug/Release clean plus `win-x64` publish, frontend production webpack, and UI copy into direct-run output folders; started PID `12632` from `G:\arr-fork\Prowlarr\_output\net8.0\win-x64\publish\Prowlarr.Console.exe`. Radarr used .NET SDK `8.0.421`, Corepack Yarn `1.22.19`, locked Yarn install, backend Debug/Release clean plus `win-x64` publish, frontend production webpack, and `_artifacts\win-x64\net8.0\Radarr` synchronization; started PID `84540` from `G:\arr-fork\Radarr\_artifacts\win-x64\net8.0\Radarr\Radarr.Console.exe`. Verified `http://localhost:9696/` and `http://localhost:7878/` both returned `200 text/html`. Test assemblies compiled but the test suite was not executed because test commands remain undocumented.
- Added R006 as the next-priority Radarr plan: backend Release Filter profiles, configurable audio language mapping/scoring/preference, audio and Custom Format mutex groups, Quality Profile bindings, and automatic sorting by Custom Format score then selected-audio score. Created `planning/radarr-release-filter-audio-scoring-design.md`, appended R006 to `planning/requirements.md`, and updated `status/current.md` so R006 is the active requirement while R001 validation remains a supporting follow-up.
- Started R006 Phase 1 in Radarr. Added backend Release Filter Profile model/repository/service, migration `244_add_release_filter_profiles`, table mapping, `/api/v3/releasefilterprofile` API resources/controllers, optional `ReleaseFilterProfileId` on Quality Profiles, backend condition-tree evaluator for non-MediaInfo fields, a download-decision specification and rejection reasons, plus evaluator fixture coverage for AND/OR, numeric comparison, and list fields. Ran `dotnet clean` Debug/Release and backend `win-x64` publish; the first publish failed on an IDE0005 unused using, then passed after removing it. Test assemblies compiled, but the test suite was not executed because test commands remain undocumented.
- Continued R006 Phase 2 in Radarr. Added MediaInfo-backed Release Filter evaluator fields for audio languages/specifications, subtitle languages, selected/preferred audio, selected-audio score, Chinese accessibility, and MediaInfo status; added `RequiresMediaInfo` detection; split Release Filter decision handling so non-interactive searches get a temporary rejection while required MediaInfo is pending and a permanent rejection only when required MediaInfo is unavailable. Added evaluator/specification fixtures for MediaInfo fields and pending/unavailable behavior. Verified with Radarr `git diff --check` and backend `win-x64` publish; output executables and `Radarr.Core.Test.dll` were produced. Test assemblies compiled, but the test suite was not executed because test commands remain undocumented.
- Continued R006 Phase 3 in Radarr. Added Audio Language Mapping model/repository/service, migration `245_add_audio_language_mappings`, `/api/v3/audiolanguagemapping` API resources/controllers, seeded Chinese aliases including `Chinese`, `Mandarin`, and `Guoyu`, and audio-track tagging fields on `ReleaseAudioInfo` for `MappedLanguage`, `LanguageTags`, and matched alias. Release Filter audio fields now use mapped language/tag values, selected audio language can resolve through mappings, `Origin` tags are added when a track matches the movie original language, and the current Chinese media preference path now uses mapped tags for decisions, sorting, and release/mediaInfo API output. Added Audio Language Mapper, Chinese preference, and Release Filter evaluator fixture coverage. Verified with Radarr `git diff --check` and backend `win-x64` publish; the first publish failed on a StyleCop using-order issue, then passed after fixing it. Output executables and `Radarr.Core.Test.dll` were produced. Test assemblies compiled, but the test suite was not executed because test commands remain undocumented.
- Completed R006 Phase 4-7 in Radarr. Added Audio Score Profile and Audio Language Preference persistence/API, configurable audio scoring rules with mutex groups, selected-audio preference evaluation, Custom Format Mutex Group persistence/API, mutex-aware Custom Format score calculation through Quality Profiles, Quality Profile bindings for Release Filter/audio score/audio preference/Custom Format mutex profiles, release API selected-audio score and breakdown fields, interactive-search selected-audio/audio-score columns plus filters/sorting, and automatic ordering by Custom Format score before selected-audio score. Verified with Radarr `git diff --check`, localization JSON parsing, backend Debug/Release clean plus `win-x64` publish, and frontend production webpack. The first backend publish failed on a StyleCop blank-line issue in `AudioScoreProfileService`, then passed after fixing it. Test assemblies compiled, but the test suite was not executed because test commands remain undocumented.
- Added the Radarr Release Scoring settings UI requested after R006: `/settings/releasescoring` is linked from Settings and provides create/edit/delete modals for Audio Language Mappings, Audio Score Profiles, Audio Language Preferences, and Custom Format Mutex Groups. Verified translation key coverage, `en.json` parsing, `git diff --check`, targeted ESLint for the new settings page, backend `win-x64` publish, frontend production webpack, synchronized `_output` into `_artifacts/win-x64/net8.0/Radarr`, restarted the running workspace Radarr process from the refreshed artifact, copied the final `_output/UI` into the running artifact UI after lint fixes, confirmed `/settings/releasescoring` returned `200 text/html`, confirmed the UI bundle and artifact localization include the new page strings, and confirmed read-only API GETs for the four configuration endpoints returned successfully. In-app browser visual verification stopped at the Radarr login page because that browser session was not authenticated.
- Fixed the discoverability gap for the new Radarr Release Scoring UI by adding `Release Scoring` to the left Settings sidebar between Custom Formats and Indexers. Rebuilt the frontend production UI, copied `_output/UI` into the running `_artifacts/win-x64/net8.0/Radarr/UI` folder, confirmed `/settings/releasescoring` still returned `200 text/html`, and confirmed the running UI bundle contains the `/settings/releasescoring` route/menu string.
- Reworked the Radarr R006 settings UI per user feedback: Custom Format Mutex Groups now live inside Custom Formats as card-style CRUD controls; the former Release Scoring page is now the Audio Formats page containing Audio Language Mappings, Audio Score Profiles, and Audio Language Preferences; Audio Language Mapping aliases now use editable input rows; Audio Score mutex groups use card controls; Audio Score rules choose mutex groups from a dropdown; and `/settings/releasescoring` redirects to `/settings/audioformats`. Verified with targeted ESLint, `en.json` parsing, `git diff --check`, and frontend production webpack. Copied `_output/UI` plus updated `en.json` into the running Radarr artifact, restarted Radarr from `_artifacts/win-x64/net8.0/Radarr` as PID 70472, confirmed `/settings/audioformats`, `/settings/customformats`, and `/settings/releasescoring` returned `200 text/html`, and confirmed authenticated read-only GETs for the five relevant configuration endpoints returned `200`.
- Refined the Radarr R006 card editors again per user screenshots: Audio Language Mappings, Audio Score Profiles, and Audio Language Preferences now render as card grids; Audio Score Mutex Groups inside the Audio Score Profile editor render as name-only cards that open a dedicated group editor with name, enabled state, and Audio Score Rule card selection; and Edit Custom Format Mutex Group now selects Custom Formats through cards instead of a checkbox list. Verified with targeted ESLint, `en.json` parsing, `git diff --check`, and frontend production webpack. Copied `_output/UI` plus updated `en.json` into the running Radarr artifact, restarted Radarr from `_artifacts/win-x64/net8.0/Radarr` as PID 82204, confirmed `/settings/audioformats`, `/settings/customformats`, and `/settings/releasescoring` returned `200 text/html`, and confirmed authenticated read-only GETs for the five relevant configuration endpoints returned `200`.
- Implemented the next Radarr R006 UI follow-ups: Audio Language Preference entry `Language Tag` is now a language dropdown with the virtual `Origin` tag, and a new Settings -> Filters page lets users create, edit, and delete Release Filter Profiles without using the search filter UI. The Filters page includes card-style profile CRUD, grouped backend filter editing, field/operator/value controls, Settings homepage navigation, and left-sidebar navigation. Verified `en.json` parsing, translation-key coverage for the new page, `git diff --check`, targeted ESLint, and frontend production webpack. Copied `_output/UI` plus updated `en.json` into the running Radarr artifact, restarted Radarr from `_artifacts/win-x64/net8.0/Radarr` as PID 59660, confirmed `/settings/filters` and `/settings/audioformats` returned `200 text/html`, and confirmed authenticated read-only GET `/api/v3/releasefilterprofile` returned `200`.
- Refined the Radarr Settings -> Filters Release Filter Profile editor so its Filter section uses the same compact nested row layout as Custom Filter: `and`/`or` group selector, add-condition/add-group icon controls, inline field/operator/value controls, nested group indentation, and sibling condition insertion. Verified with targeted ESLint, `git diff --check`, and frontend production webpack. Copied `_output/UI` into `_artifacts/win-x64/net8.0/Radarr/UI`, restarted Radarr from the artifact as PID 29772, confirmed `/settings/filters` returned `200 text/html`, and confirmed the running UI bundle contains the new row-editor classes.
- Removed the group-header add-condition button from Radarr Custom Filter and Release Filter editors so condition rows are added only from existing row `+` controls; Release Filter groups now keep at least one row and disable deletion of a group's final child. Verified with targeted ESLint, `git diff --check`, and frontend production webpack. Copied `_output/UI` into `_artifacts/win-x64/net8.0/Radarr/UI`, restarted Radarr from the artifact as PID 72160, and confirmed `/settings/filters` returned `200 text/html`.
- Fixed a Radarr R006 behavior bug where the earlier hard-coded Chinese media availability decision specs still globally rejected releases with neither Chinese audio nor Chinese subtitles even when no Release Filter Profile was bound to the movie's Quality Profile. Deleted those global specs and their dedicated fixture, added Release Filter specification coverage for the no-bound-profile case, verified with `git diff --check` and backend `win-x64` publish, synchronized `_output/net8.0/win-x64/publish` into the running `_artifacts/win-x64/net8.0/Radarr` folder, restarted Radarr as PID 84276, and confirmed `/api/v3/system/status` returned `200`.
